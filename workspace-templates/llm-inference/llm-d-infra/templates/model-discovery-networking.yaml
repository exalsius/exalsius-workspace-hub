# HTTPRoute for model discovery service
# The model discovery service is used by the open-webui to discover which models are available for inference.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: "model-discovery-httproute"
  namespace: {{ .Release.Namespace }}
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: llm-d-inference-gateway
      sectionName: internal
  rules:
    - backendRefs:
        - name: model-registry
          port: {{ .Values.modelRegistry.service.port }}
      matches:
        - path:
            type: PathPrefix
            value: /v1/models