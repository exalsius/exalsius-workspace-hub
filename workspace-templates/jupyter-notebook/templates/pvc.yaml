apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "jupyter-notebook.pvcName" . }}
  namespace: {{ .Values.global.deploymentNamespace | default "default" }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.resources.storageGb }}Gi