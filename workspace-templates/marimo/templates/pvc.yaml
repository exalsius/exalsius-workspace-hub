apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "marimo.pvcName" . }}
  namespace: {{ .Values.global.deploymentNamespace | default "default" }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.storageGb }}Gi